
const initialState = {
    gifd: [],
    loading: false,
    error: ''
}

const reducer = (state , action) =>{
    switch(action.type) {
        default:
            return "state"
    }
}

export default reducer;